> 本工具提供函数如下

```js
/**
 * 加法
 * @param {*} a
 * @param {*} b
 * @returns
 */
export function sum(a, b) {
  return a + b;
}
```

```js
/**
 * 减法
 * @param {*} a
 * @param {*} b
 * @returns
 */
export function sub(a, b) {
  return a - b;
}
```
